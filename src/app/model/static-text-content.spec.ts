import { StaticTextContent } from './static-text-content';

describe('StaticTextContent', () => {
  it('should create an instance', () => {
    expect(new StaticTextContent('', '', '', '', '', '')).toBeTruthy();
  });
});
